/**
 * Contains the domain rules and value objects for the Give or Take estimation quiz.
 *
 * <p>This package must contain no {@code android.*} imports. Keeping domain logic independent
 * of the Android framework allows it to run in ordinary JVM unit tests, which provide faster
 * feedback and make scoring and calibration rules easier to test in isolation.</p>
 */
package de.christiankorn.giveortake.core;
